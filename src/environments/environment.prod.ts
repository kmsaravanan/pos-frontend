export const environment = {
  production: true,
  gstEnabled: false,
  businessName: 'Marudham Hardwares',
  apiUrl: 'https://invoicingsytem.onrender.com/invoicesys/api' // Leave empty! We will inject this via Docker/env vars on Render
};